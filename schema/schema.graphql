type Mutation {
  createUser(name: String!): User
  deleteUser(id: ID!): User
}

type Post {
  content: String
  id: ID
  title: String
}

type Query {
  post(id: ID!): Post
  postCount: Int
  posts: [Post!]
  user(id: ID!): User
  users: [User!]
}

type User {
  id: ID
  name: String
}