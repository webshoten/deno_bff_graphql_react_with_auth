# Deno Deploy へのデプロイ用ワークフロー
# https://github.com/denoland/deployctl

name: Deploy to Deno Deploy

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  id-token: write # Deno Deploy に必要

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      # フォーマットチェック（オプション）
      # - name: Verify formatting
      #   run: deno fmt --check

      # Linter（オプション）
      # - name: Run linter
      #   run: deno lint --ignore=public/generated/

      # テスト（オプション）
      # - name: Run tests
      #   run: deno test -A

      # フロントエンドのビルド
      - name: Build frontend
        env:
          DENO_ENV: production
        run: deno task build

      # Deno Deploy へデプロイ
      - name: Deploy to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: "deno-bff-gr-65"
          entrypoint: "src/server.ts"
          include: |
            src
            dist
            public
            import_map.json
            deno.json
